{
	"name": "12months",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
      "git":"git add . && git commit -m \"auto push\" && git push origin master ",
		"build": "vite build",
		"preview": "npm run build && wrangler pages dev",
		"prepare": "svelte-kit sync || echo ''",
		"deploy": "npm run build && wrangler pages deploy",
		"deploy:preview": "npm run build && wrangler pages deploy --branch preview"
	},
	"devDependencies": {
		"@svelte-put/movable": "^4.0.0",
		"@sveltejs/adapter-auto": "^6.0.0",
		"@sveltejs/adapter-cloudflare": "^7.0.4",
		"@sveltejs/kit": "^2.16.0",
		"@sveltejs/vite-plugin-svelte": "^5.0.0",
		"svelte": "^5.0.0",
		"vite": "^6.2.6",
		"vite-plugin-devtools-json": "^0.2.0",
		"wrangler": "^4.22.0"
	},
	"dependencies": {
		"dexie": "^4.0.11",
		"firebase": "^11.10.0",
		"svelte-dnd-action": "^0.9.65"
	}
       
}
